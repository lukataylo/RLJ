# External verification stack — non-LLM tests that gate the build.
pytest>=8
pytest-json-report>=1.5
jsonschema>=4.21
pandera>=0.18
pandas>=2.0
requests>=2.31
httpx>=0.27
pyyaml>=6
networkx>=3
# statistics for the research/flywheel/benchmark backtests (paired Wilcoxon, etc.)
scipy>=1.11
# Google OR-Tools — third-party PDPTW baseline used by the optimality/benchmark tests
# and as a portfolio member in routing/solver.py (import-guarded in app code).
ortools>=9.8
# e2e (browser tests). Install browsers once with: ./.venv/bin/python -m playwright install chromium
pytest-playwright>=0.5
# auth layer — needed so CI can run tests/e2e/test_auth.py (psycopg omitted; tests use sqlite).
sqlalchemy>=2
passlib[bcrypt]
bcrypt>=4.0,<4.1  # passlib 1.7.x is incompatible with bcrypt>=4.1
pyjwt
