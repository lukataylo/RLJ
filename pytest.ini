[pytest]
testpaths = tests
markers =
    e2e: end-to-end browser tests (need a running stack + chromium)
    slow: longer-running tests (backtests over many scenarios)
    data: data-quality tests
filterwarnings =
    ignore::DeprecationWarning
