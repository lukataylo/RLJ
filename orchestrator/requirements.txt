fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-dotenv>=1.0
# --- auth layer (email+password login, JWT, users in DB) ---
sqlalchemy>=2
passlib[bcrypt]
bcrypt>=4.0,<4.1  # passlib 1.7.x is incompatible with bcrypt>=4.1
pyjwt
psycopg[binary]   # Postgres driver for Railway (DATABASE_URL=postgresql://…)
